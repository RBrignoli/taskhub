// const express = require("express");
// const router = express.Router();
// const sprintController = require("../controllers/sprintsControl")

// // Get all sprints
// router.get("/", sprintController.listSprints);

// // Get single sprint
// router.get("/:id", sprintController.getSprint);

// // Create a new sprint
// router.post("/", sprintController.createSprint);

// // Update a sprint by its ID
// router.patch("/:id", sprintController.updateSprint);

// // Delete a sprint by its ID
// router.delete("/:id", sprintController.deleteSprint);

// module.exports = router;
